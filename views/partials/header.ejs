<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>BookManiac</title>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.4.1/css/bootstrap.min.css">
    <link rel="stylesheet" href="/stylesheet/app.css">
    <script src="https://kit.fontawesome.com/db5f6fcbe1.js" crossorigin="anonymous"></script>
    <link href="https://fonts.googleapis.com/css2?family=Source+Sans+Pro:wght@300;400;600&display=swap" rel="stylesheet">
</head>
<body>
    <nav class="navbar navbar-dark bg-dark navbar-expand-md pt-0 pb-0">
        <div class="container">
            <a  class="navbar-brand"><i class="fas fa-book-reader"></i> BookManiac </a>
            <ul class="navbar nav mr-auto">
                <li class="nav-item active">
                    <a href="/books" class="nav-link">Home</a>
                </li>
                <li class="nav-item">
                    <a href="/books/new" class="nav-link">Add New Book</a>
                </li>
            </ul>
            <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#hiddenitems" aria-controls="hiddenitems" aria-expanded="false">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="hiddenitems">
                <ul class="navbar-nav ml-auto">
                    <% if(!currentUser){ %>
                        <li class="nav-item">
                            <a href="/login" class="nav-link">Log In</a>
                        </li>
                        <li class="nav-item">
                            <a href="/register" class="nav-link">Sign Up</a>
                        </li>
                    <% } else { %>
                        <li class="nav-item">
                            <a class="nav-link">Signed In As <%= currentUser.username %></a>
                        </li>
                        <li class="nav-item">
                            <a href="/logout" class="nav-link">Log Out</a>
                        </li>
                    <% } %>
                

                </ul>
            </div>
        </div>
    </nav>
    <% if(error.length > 0){%>
    <div class="container mt-3 p-0">
        <div class="alert alert-danger">
                <%=error%>
        </div>   
    </div>
    <% } %>
    <% if(success.length>0){ %>
    <div class="container mt-3 p-0">
        <div class="alert alert-success">
                <%=success%>
        </div>
    </div>
    <% } %>